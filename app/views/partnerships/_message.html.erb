<%= turbo_frame_tag 'messages-list' do %>
  <div class='grid grid-cols-4 gap-y-2'>
    <% messages.each do |message| %>
      <div class="<%= message_class(message) %>" <%= dom_id(message) %>>
        <p><%= simple_format(h(message.body)) %></p>
      </div>
    <% end %>
  </div>
<% end %>
